# OpenAI API Key (optional)
# If you want to use real AI for generating event descriptions,
# get your API key from https://platform.openai.com/api-keys
# and add it here. If not provided, the system will use template-based generation.
OPENAI_API_KEY=your_openai_api_key_here
